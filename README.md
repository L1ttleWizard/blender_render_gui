# Blender Render GUI

> Простой и удобный графический интерфейс для фонового рендеринга в Blender под Windows.

Это приложение-обертка над командной строкой Blender, созданное для художников и всех, кому нужно удобно запускать рендеры, не запоминая консольные команды.

![Скриншот вашего приложения](screenshot.png)

## ✨ Ключевые особенности

*   **Удобный выбор файлов**: Легко указывайте путь к `blender.exe` и вашему `.blend` файлу через проводник.
*   **Сохранение настроек**: Приложение запоминает выбранные пути и настройки между запусками.
*   **Живой вывод консоли**: Следите за процессом рендеринга в реальном времени прямо в окне приложения.
*   **Полный контроль**: Управляйте основными параметрами рендера через чекбоксы и поля для ввода (анимация, диапазон кадров, движок, потоки).
*   **Превью команды**: Мгновенно видите, какая команда будет отправлена в Blender.
*   **Управление процессом**: Запускайте, останавливайте рендер и открывайте Blender в обычном режиме одной кнопкой.
*   **Никаких зависимостей**: Написано на стандартной библиотеке Python (Tkinter), не требует установки дополнительных модулей.

## 🚀 Как использовать

### Для обычных пользователей (Рекомендуемый способ)

1.  Перейдите на страницу **[Releases](https://github.com/ВАШ_НИКНЕЙМ/ВАШ_РЕПОЗИТОРИЙ/releases)**.
2.  Скачайте последнюю версию `.exe` файла (например, `render_gui.exe`).
3.  Запустите файл. Никакой установки не требуется!

### Для разработчиков (Запуск из исходного кода)

Если вы хотите запустить скрипт напрямую или внести изменения:

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/ВАШ_НИКНЕЙМ/ВАШ_РЕПОЗИТОРИЙ.git
    ```
2.  Перейдите в папку проекта:
    ```bash
    cd ВАШ_РЕПОЗИТОРИЙ
    ```
3.  Запустите скрипт (внешние библиотеки не требуются):
    ```bash
    python render_gui.py
    ```

## 🛠️ Как собрать `.exe` из исходников

Для сборки используется **PyInstaller**.

1.  Установите PyInstaller:
    ```bash
    pip install pyinstaller
    ```
2.  Выполните команду сборки, находясь в папке проекта:
    ```bash
    pyinstaller --onefile --windowed --icon="icon.ico" render_gui.py
    ```
    *   `--onefile` — собрать всё в один файл.
    *   `--windowed` — убрать консольное окно при запуске.
    *   `--icon="icon.ico"` — (опционально) добавить иконку.

3.  Готовый `.exe` файл будет ждать вас в папке `dist`.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности смотрите в файле `LICENSE`.
